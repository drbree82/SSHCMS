# Ticket 001.5b — Strengthen feed/render tests only

## Read

- spec.md
- tests/test_render.py
- sshcms/render.py

## Modify only

- tests/test_render.py

## Do not modify

- sshcms/render.py
- sshcms/parser.py
- sshcms/content.py
- sshcms/state.py
- sshcms/agent.py
- sshcms/tui.py
- sshcms/app.py

## Goal

Strengthen the feed generation test so it verifies actual feed output structure instead of only checking that generate_feed() does not crash.

## Required tests

Add or improve tests so they verify:

- feed output contains `<feed`
- feed output contains at least one `<entry`
- feed output contains a post title
- feed output contains post content or summary text
- output file is written

## Run

uv run pytest -v

## Success criteria

- git diff is not empty
- only tests/test_render.py changed
- tests pass
